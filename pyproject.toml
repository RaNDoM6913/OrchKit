[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-workflow-orchestrator"
version = "0.1.0"
description = "ChatGPT-first durable workflow orchestrator"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
orch = "orch.cli:main"
